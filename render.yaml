services:
  - type: web
    name: reverie-wellness
    runtime: node
    plan: free
    nodeVersion: "20"
    buildCommand: npm install --build-from-source
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3000"
